# FastAPI and ASGI
fastapi==0.109.0
mangum==0.17.0
python-multipart==0.0.9

# HTTP client (also used for DynamoDB via lightweight client)
httpx==0.26.0

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0

# RSS generation
feedgen==1.0.0

# Note: boto3 removed - using custom lightweight DynamoDB client
# This saves ~70MB in package size and improves cold starts
