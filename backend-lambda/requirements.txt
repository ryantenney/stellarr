# FastAPI and ASGI
fastapi==0.109.0
mangum==0.17.0

# HTTP client
httpx==0.26.0

# Configuration
pydantic==2.5.3
pydantic-settings==2.1.0

# RSS generation
feedgen==1.0.0

# AWS - boto3 is included in Lambda runtime, but adding for local dev
boto3>=1.34.0
